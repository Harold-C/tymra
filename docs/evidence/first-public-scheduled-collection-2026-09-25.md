# First public scheduled collection, 2026-09-25

Status: **phase one running**. The production Scheduler has enabled only the bounded weekly `public_holidays_nz` and `mbie` schedules, and each completed its first scheduled Job. The prepared daily `rto_calendars` schedule is disabled pending a second distinct successful weekly cycle for both first-stage sources. This is a limited recurring production pilot, not nationwide completeness or long-term stability acceptance.

## Release and recovery

- Code commit `622a979dbdd96f5a981d094e80c57b4a1476a341` on `release/tymra-public-canary-20260925`; source archive `/srv/apps/tymra/releases/first-public-schedules-20260925/source.tar.gz` SHA-256 `06dd1d2e3abf881391bb1076b4e69479663db0709683e463e2299101e0af0ff7`. The native amd64 production image is `tymra:first-public-schedules-20260925`, exact ID `sha256:c3ee8faaf0503905321dea2f7dc2f64d2f5b3b1e058c7ab1e5aa0d51adaca306`. The build log SHA-256 is `67b57508026d7af4b60ec2dedb1acaf165cd91910e607334b2569497f2ff3a5c`. The new Compose SHA-256 is `34285cc883d9c6500cc834f0799dac9dd1f48aaa025e52367af13d1aae898ea9`; relative to the prior release it changes only the app image name and source build context. Admin-only Web remains on `sha256:ed9e56e89aab5ed82211660d893aae424d2a5e9e41d5e3f9dae2056bed7b9417`.
- Before production mutation, `/srv/apps/tymra/backups/first-public-schedules-20260925/` captured the protected environment, prior Compose, PostgreSQL custom-format dump and evidence volume. All four checksums and archive listings passed (657 database archive entries, seven evidence archive entries). The existing Worker/API image `sha256:1f9234adb27d8e01792f6aefe6fd0c98cf9a38031fbeb5bd15ac1fb45becda77` and release Compose remain available.
- After the first scheduled Jobs, `/srv/apps/tymra/backups/first-public-schedules-20260925/post-activation/` captured the active environment and Compose, a fresh PostgreSQL dump (SHA-256 `1833193795b053662b885bae921637fd7a82bff6b7cdb8014969613377fc38c8`) and evidence archive (SHA-256 `ab04ebcb4f91da224eaa01a52f3d9b14e7bfbec332acdf374ca0910ef26cb68e`). All checksums and archive listings passed. Backup directories are root-only; files are mode `0600`. A separate scratch-database restore was not run.
- Only `PROD_SCHEDULER_ENABLED` changed from false to true in the protected root-owned `0600` environment file; an exact reverse comparison verified all other bytes, ownership and mode unchanged. No credential value was printed. The final Compose render retained high-frequency scheduling, new checks, internal on-demand, customer funnel and billing disabled, with Admin-only access true. No migration or general/demo seed ran; 33 successful migrations remain.
- To stop new collection, use the guarded `schedule:sources:disable` command for the two active sources, then stop only the `scheduler` Compose service and restore `PROD_SCHEDULER_ENABLED=false` from the protected pre-activation configuration. Preserve the accepted database and evidence volume. If the runtime itself requires rollback, recreate only Worker/API using `/srv/apps/tymra/releases/public-canary-20260925-v2/compose.yml` and its retained image. The rollback material was verified but rollback was not executed.

## Bounded automatic acceptance

The three exact source-bound schedules were created disabled by `schedule:first-public:prepare`; the read-only plan found all three healthy, approved and free of blockers. The Scheduler runs only these exact source/key/type/queue/cadence/payload combinations in production and fails closed on any other enabled schedule. Each queued pilot Job has `maxAttempts=1`, a rolling 31-day window, a 30-second abort signal, 2 MB response ceiling and combined business-result cap. The holiday and MBIE schedules each permit one upstream request and at most 20 business results. The prepared ChristchurchNZ schedule permits up to three requests and 30 results, but is not enabled.

| Source | Schedule | First Job / CollectionRun | First result | Next scheduled time (UTC) |
| --- | --- | --- | --- | --- |
| `public_holidays_nz` | `first-public-holidays-weekly` | `cmugb0gv30000nu07nkytem6a` / `cmugb0hby0001oc07ys8l1y9h` | `SUCCEEDED`, one request, one existing signal refreshed, zero failures or row growth | 2026-10-02 01:50:36 |
| `mbie` | `first-mbie-adp-weekly` | `cmugb31k90001nu07cd2ea093` / `cmugb31vj0003oc07ykiusoqi` | `SUCCEEDED`, one request, 20 signals saved, two unchanged and 18 new, zero failures | 2026-10-02 01:52:36 |
| `rto_calendars` | `first-christchurchnz-daily` | None | Prepared but disabled | None |

Both successful Jobs attempted once; their CollectionRuns reported unchanged source configuration and schedule snapshots. The holiday source has one business signal; MBIE has 20. The scheduler, Worker and API run the exact new image with zero restarts; API/Web are healthy. Internal `/worker/health` returned HTTP 200 with PostgreSQL, Redis and Argus ready, queue depth and failed count zero, `scheduler.enabled=true`, and no alerts. The final production database contained six `SUCCEEDED` Jobs total and no pending, running or failed Job. There are exactly three schedule rows, two enabled and one disabled.

## Next gate and limits

The two weekly schedules must each complete their second distinct scheduled cycle after 2026-10-02 01:52 UTC. Recheck Job/CollectionRun status, source data freshness, request/result ceilings, duplicate business rows and links, queue, alerts, backups and closed feature switches. Only then enable the already-prepared ChristchurchNZ schedule and validate its first daily Job, or leave it disabled and report a blocker. No extra source-site probe is needed for this gate. The 20-result MBIE cap is a pilot ceiling and may omit other published regions; full national completeness is not claimed. Lincoln's restricted acceptance, RBNZ's known parser failure, OTA accounts, customer access, Stripe, SMTP and membership remain outside this rollout.
