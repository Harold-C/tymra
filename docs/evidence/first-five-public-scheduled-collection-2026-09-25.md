# Five public-source production Jobs, 2026-09-25

Status: **first round completed; automatic collection paused for review**. The five channels are `public_holidays_nz`, `mbie`, `rto_calendars` (ChristchurchNZ), `geonet`, and `stats_nz`. Each has one successful Scheduler-enqueued production Job. The first two Jobs were accepted earlier on 2026-09-25; the other three were added and run once in this expansion. All five schedules are now disabled, `PROD_SCHEDULER_ENABLED=false`, and the Scheduler container is stopped. This is a bounded first round, not multi-cycle stability or complete channel coverage.

## Release and recovery

- Final code commit: `c82690ad04699a36ae0b72ed233fcb3dca12b31f` on `release/tymra-public-canary-20260925`, pushed to origin. Exact source archive `/srv/apps/tymra/releases/first-five-public-20260925-v4/source.tar.gz` SHA-256 `7cba7d0c45fa5bbd9d1f9fb8dafc7daf442763b53f1a4d2d21a0931976312502`; Compose SHA-256 `2b4d0c325f6ca0c9cbd51e3307afb52957c8fed13aaa54a93eac9f2fb71ddb8e`; build log SHA-256 `87f23024e45276d4e6e7916cedf698b753fc5d00ccf6276522c8660adc9545cb`.
- Worker/API run native amd64 image `tymra:first-five-public-20260925-v4`, exact image ID `sha256:97b5bc5ccdf09950d6326337518e8f1edb1cb0b56b8db4dcdafe846c202f2768`. The Admin-only Web remains on `sha256:ed9e56e89aab5ed82211660d893aae424d2a5e9e41d5e3f9dae2056bed7b9417`; the Scheduler has exited normally. Image `tymra:first-public-schedules-20260925` and its Compose release remain available as runtime rollback material.
- Root-only pre-activation, pre-hash-repair, and post-acceptance snapshots are in `/srv/apps/tymra/backups/first-five-public-20260925/`. Each contains the protected environment, Compose, PostgreSQL custom-format dump, evidence-volume archive, and verified checksums. The post-acceptance database dump SHA-256 is `0007e25e2751c5f644598c1308cba72eda5b63fb026cff6440641c5e8c2dc91d`; evidence archive SHA-256 is `46d4bd678478e2387751ec4681be1b71a0d1ab877a0cc305f89013e0d68be92e`. Archive listings passed (672 database entries, seven evidence entries). A scratch restore was not run. Directories are root-only `0700`, files `0600`.
- No migration or general/demo seed ran in production; 33 migrations are successful and none failed. The two added non-demo source records came only from the formal source registry. The sole final environment change relative to the earlier running pilot was `PROD_SCHEDULER_ENABLED` from true to false; the file remains root-owned `0600`, with all other bytes preserved. Other launch switches remained closed.
- For runtime rollback, keep all schedules disabled and Scheduler stopped; recreate only Worker/API from `/srv/apps/tymra/releases/first-public-schedules-20260925/compose.yml` and its retained image. Preserve the accepted production database and evidence volume. Restoring the pre-activation database would discard this round's results and needs separate review. Recovery material was checksum- and archive-verified, but a rollback and scratch restore were not executed.

## Bounded first round

The production Scheduler accepted only five exact source/key/type/queue/cadence/payload combinations. Every Job had `maxAttempts=1`, a rolling 31-day window, 30-second collection abort signal, 2 MB response ceiling, and combined result cap. These three new channels use direct official public HTTP adapters; they did not create Argus Jobs or use Argus evidence/ACK. The Worker performed the real collection and persistence. No Booking, Airbnb, Synix account, customer entry, generic seed, or high-frequency schedule was used.

| Source | Job / CollectionRun | Requests / saved results | Persisted business state |
| --- | --- | --- | --- |
| `public_holidays_nz` | `cmugb0gv30000nu07nkytem6a` / `cmugb0hby0001oc07ys8l1y9h` | 1 / 1 | One existing market signal and link refreshed; no row growth |
| `mbie` | `cmugb31k90001nu07cd2ea093` / `cmugb31vj0003oc07ykiusoqi` | 1 / 20 | 20 market signals and links; 18 new in this Job |
| `rto_calendars` | `cmugdkznv0000l3079csf67i2` / `cmugdl0ah0001qm088givzi9a` | 3 / 30 | 15 source events, 30 occurrences, 15 event links and 30 occurrence links |
| `geonet` | `cmugdma1e0001l3075oumy8sq` / `cmugdma49006wqm08bkcba0d3` | 1 / 20 | 20 market signals and links |
| `stats_nz` | `cmugdnkdj0002l307v3iicrww` / `cmugdnkro0096qm08vhphsl7j` | 1 / 1 | One market signal and link |

All five Jobs and CollectionRuns ended `SUCCEEDED`, with one attempt, zero collection failures, unchanged source/schedule snapshots during the run, and no duplicate external IDs in the new business rows. All nine production Jobs in the database are `SUCCEEDED`; none is pending, running, failed, or dead-lettered. After this round, the database has exactly five schedule rows, all disabled with `nextRunAt=null`.

The five runs have 74 stored `RawArtifact` payloads in total. Recalculation of the canonical JSON SHA-256 from each stored payload matched all 74; none of these artifacts has a parser failure. These hashes attest to Tymra's retained parsed payloads, not the upstream HTTP response bytes. The GeoNet run originally stored 15 hashes of pre-PostgreSQL floating-point values that did not match its retained JSONB payload. The Scheduler was stopped; a pre-repair snapshot was made. Read-only analysis showed each mismatch was explained by a one- or two-ULP change in depth or magnitude during JSONB roundtrip. A transaction scoped to the exact GeoNet run updated only those 15 hashes to the retained payload hashes, then verified 20/20. No source was fetched again. The Worker code now hashes the database-returned payload inside the artifact write transaction; a disposable-database integration test covered that readback path.

## Operating boundary and review

The final Worker/API `/worker/readiness` and `/worker/health` returned HTTP 200; PostgreSQL, Redis, and Argus were healthy, `scheduler.enabled=false`, and alerts were empty. Worker/API and the unchanged Admin-only Web have zero restarts. The Worker still mounts `tymra_argus_evidence` read/write; a temporary marker was written, read, and removed after the final container recreation. `PROD_HIGH_FREQUENCY_SCHEDULER_ENABLED`, `PROD_ACCEPT_NEW_CHECKS`, `PROD_INTERNAL_ON_DEMAND_ENABLED`, `PROD_CUSTOMER_FUNNEL_ENABLED`, and `PROD_BILLING_ENABLED` remain false; `PROD_ADMIN_ONLY_ACCESS` remains true.

Review before resuming: GeoNet reached the 20-result cap after its first earthquake request, so the volcano endpoint was not visited; the channel is only partially covered. MBIE's 20-result limit is a pilot cap, not nationwide completeness. Stats NZ produced one current indicator; its monthly publication rhythm may warrant a lower polling frequency. Ministry of Education school holidays were considered but not activated because the production Node health check returned HTTP 307; RBNZ remains unaccepted due to its known parser failure. No second scheduled cycle, long-running freshness, sustained capacity, OTA account flow, customer flow, Stripe, SMTP, or membership was accepted here. The prior two-cycle ChristchurchNZ enablement automation was removed when Harold explicitly expanded the first round to five channels.
