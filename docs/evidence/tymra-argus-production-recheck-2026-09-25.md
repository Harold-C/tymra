# Tymra → Argus production recheck, 2026-09-25

Status: **accepted for one bounded public, read-only Lincoln University Job through the existing production Worker/API**. No production image or configuration was changed. This does not validate scheduled collection, OTA Profiles, customer access, RBNZ, or other Argus connectors.

## Release, configuration and recovery

- Tymra production Worker/API: `tymra:public-canary-20260925-v2`, exact image ID `sha256:1f9234adb27d8e01792f6aefe6fd0c98cf9a38031fbeb5bd15ac1fb45becda77`, built from code commit `6d948dda4e76cb87416661e2ce4b4863341be52b`. Compose release: `/srv/apps/tymra/releases/public-canary-20260925-v2/compose.yml`. The Admin-only Web remains on image ID `sha256:ed9e56e89aab5ed82211660d893aae424d2a5e9e41d5e3f9dae2056bed7b9417`. The Worker runs and API/Web are healthy; PostgreSQL and Redis are healthy. No release or migration was necessary; the database retained 33 successful migrations and zero unfinished migrations.
- The running Argus container image ID is `sha256:d70496755720b0fc3d7dd3a5651850ea6c0dfabc515a0112a265e2e81d19835c`; its `org.opencontainers.image.revision` label is `7df378fda31453dc6da724fb9fd7892cdfe99abf`. Argus was inspected only; its service and credentials were not changed.
- Production origin is `https://api.argus.nz`. The protected Mac mini handoff file and Tymra's root-owned `production.env` both have mode `0600`. A nonce-based HMAC comparison confirmed that the production file, Worker and API read the same origin and token as the handoff file. No token value or digest was printed or saved. No environment variable changed. The local development `argus.test` configuration was untouched.
- Before the Job, `/srv/apps/tymra/backups/argus-recheck-20260925/` captured the protected environment, active Compose, PostgreSQL custom-format dump and evidence volume archive. Its four checksums passed, the dump listed 672 entries and the archive was readable. After acceptance, `/srv/apps/tymra/backups/argus-recheck-20260925/post-acceptance/` captured a fresh database dump and evidence archive: SHA-256 `aa76eb36195fcb98275f46aea0947d5ab9ff10ef28e860426eb51ec82536d903` and `ab04ebcb4f91da224eaa01a52f3d9b14e7bfbec332acdf374ca0910ef26cb68e`. Both checksums and archive listings passed. Backup directories are root-owned, mode `0700`; files are mode `0600`. The archived acceptance evidence files independently matched their stored hashes. A separate scratch-database restore was not run.
- If collection fails later, stop new collection with the existing `release:rollback --confirm DISABLE_COLLECTIONS` command in the Worker container, then use the retained Compose release and exact image to restore only Worker/API. Preserve the accepted production database and evidence volume. Restoring the pre-Job dump would discard accepted records and must be assessed against the ACKed remote Job. The rollback material was checked but the rollback was not executed.

## Production checks and bounded Job

- TLS-verified Node `fetch` from the actual production Worker returned HTTP 200 for `/health` (`healthy=true`, `active_tasks=0`), `/readiness` (`ready=true`, `active_tasks=0`) and `/openapi.yaml` (YAML, 305,785 bytes, Job and ACK routes present). With the `tymra-prod` token, a randomly generated absent Job returned HTTP 404 `JOB_NOT_FOUND`; absent account and runtime routes each returned HTTP 403 `FORBIDDEN`. The token grants Job/evidence/handoff capability without account or runtime access.
- The existing healthy, non-demo `christchurch_university_dates` source retained its `lincolnAcceptanceOnly` guard and zero enabled schedules. Production preflight was ready. Exactly one parent Job was queued through Tymra's production CLI for `lincoln-university-key-dates`, workflow `collect_key_dates`, academic year 2026, the fixed 2026-01-01 through 2026-12-31 window, `--limit 2` and an idempotency key checked as absent beforehand. Its parent ID is `cmug7sipf0000mrilny30xwdj`; Argus Job is `job_6a6a3e19bb5e930470122e066a1f2443`; Tymra CollectionRun is `cmug7sjca0001mr08rinx4fag`. No OTA Profile or account was used.
- The parent Job ended `SUCCEEDED` on its first attempt, `ArgusExecution` ended `COMPLETED`, and CollectionRun ended `SUCCEEDED` with `successCount=2`. The completed Argus result had `capture=success`, `readonly_only=true`, `external_side_effects_performed=false`, `academicYear=2026`, 105 key dates, schema `lincoln-university-key-dates.collect_key_dates` version `1.0.0`, and result SHA-256 `f54dadcbdac51212e4dd9d235910859eb88d8befe0d2ae321a0ad422838fb0ba`. Tymra updated two existing distinct `UNIVERSITY_CALENDAR` signals and linked them to the new run; the source still has exactly two business rows, with no duplicate. No `argus-evidence:` raw-artifact references remain.
- Tymra copied two evidence files to its `/argus-evidence` persistent volume before ACK. Re-reading their bytes from disk and the post-acceptance backup gave the same values as Argus metadata: HTML 1,384,618 bytes, SHA-256 `3a0376b9fb12e5885ce2fba760587ff308010d9f1691cfa9b35863158401c1f1`; screenshot 1,015,016 bytes, SHA-256 `e7f5b5dfed198b0acd54e4f9876049a3a3463c228f82f7d44d935430b4f48b59`.
- Tymra's normal post-persistence path ACKed the Argus result. A separate authenticated read found delivery status `PURGED`; result GET returned HTTP 410 `RESULT_PURGED` with the expected result hash, and both remote evidence GETs returned HTTP 404. Argus readiness remained 200 with zero active tasks.

## Final operating boundary

The internal API `/worker/health` returned HTTP 200 with PostgreSQL, Redis and Argus ready, `scheduler=false`, queue depth 0, failed queue count 0 and alerts 0. Worker/API and Admin-only Web retain their exact images. The evidence volume remains mounted and writable; its files survived in the verified post-acceptance backup. No Scheduler container or enabled source schedule exists. `SCHEDULER_ENABLED`, `HIGH_FREQUENCY_SCHEDULER_ENABLED`, `ACCEPT_NEW_CHECKS`, `INTERNAL_ON_DEMAND_ENABLED`, `CUSTOMER_FUNNEL_ENABLED` and `BILLING_ENABLED` remain false; `ADMIN_ONLY_ACCESS` remains true. No customer/public registration, Stripe, SMTP or membership capability was enabled.

The scoped Tymra token cannot list Argus-wide Jobs. The checked acceptance Job has no retained remote result or evidence, and Argus reported zero active tasks; an Argus-wide historical Job inventory was not independently established. RBNZ's previously reported `PARSING_ERROR` was not retested. Other connectors and long-running scheduled operation remain unverified.
