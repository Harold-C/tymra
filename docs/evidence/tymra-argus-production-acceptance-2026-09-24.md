# Tymra → Argus production acceptance, 2026-09-24

Status: **accepted for the restricted `collection` Worker/API profile only**. Scheduler, customer intake, public registration, internal on-demand, billing, SMTP and membership remain disabled. The Admin-only Web release remains in place. This acceptance covers the bounded Lincoln University 2026 path, not other connectors.

## Release identity and recovery

- Tymra code commit: `f9968379fe31f81a43d2d5f054d96b2eab714bdc`. The amd64 candidate `tymra:argus-prod-20260924-v3` has image ID `sha256:623a91d4f1d5c22b3506532d547e1fd1be0c4969cfde3b89ed0902a2e5a39bfc`, source archive SHA-256 `776606fd7181da35c7a8d7c063f95058d5e7d63427739a81e60bf8f08bf4a990`, and build-log SHA-256 `ac31b34c38b456e0f455c1ebf2d9ee499c4eaa6e5b19ae3a83e3e35a78645604`. Its source archive is the exact prior v2 archive plus five task files. Existing Admin Web continues on `sha256:ed9e56e89aab5ed82211660d893aae424d2a5e9e41d5e3f9dae2056bed7b9417`; it was not rebuilt or replaced.
- Argus code commit: `ade443084651be22f7a4c6cc998febd1ac7f6cb9`, release tag `argus-release-20260924-2`, private registry identity `ghcr.io/harold-c/argus@sha256:1787dfbba9907f463403f204e37841151f9ca7adc6f9dd059756fc03c5dc76d0`. The exact registry image was exported privately and checked by archive, saved config and 12 layer hashes. The Mac mini's containerd store identifies the loaded image as `sha256:5d529b6fa6f8cf79a8a073c11c18b1186257567d5c21d5b96c35b653e1a5d693`; the saved config digest is `sha256:c17856a8799b316ab4df4c4242a7cb50e72b3e6b6069a5149baf145c70c52b2c`. The preceding Argus image `sha256:7124b0e6185bf5c9fd82370300fde18d90b81589caee1faf51f45f3d43b4064d` is retained.
- Before the Tymra change, `/srv/apps/tymra/backups/argus-prod-20260924-v3/` captured the original environment, Compose, Web image and PostgreSQL dump, with `SHA256SUMS` verified. The database dump was readable. The environment file remained `0600 root:root`; only `PROD_ARGUS_API_BASE_URL` and `PROD_ARGUS_API_TOKEN` changed. No token value appears here.
- After acceptance, `/srv/apps/tymra/backups/argus-prod-20260924-v3/post-acceptance/` contains a PostgreSQL dump (SHA-256 `43eba8347d480ac83508f7946e1246276b336ebdcda15a4add7090434dacb8e0`) and evidence-volume archive (SHA-256 `9344341712da14c634cfec4897e1c2bc583c1d1d1e0ec43c1319f1ff086c30aa`). Both checksums and archive listings passed. An isolated database restore recovered 33 migrations, the successful acceptance Job and two signals; the scratch database was removed. The archived evidence files matched both Argus hashes.
- Argus's coordinated pre-release backup includes the still-available Job result and was restored into isolated database and volume stores. The result hash and Job statuses matched; the temporary stores were removed. Its encrypted off-host image is `/Users/haroldchen/Development/argus/runtime/production-backups/argus-prod-20260924-pre-header-release.sparseimage` (SHA-256 `b548700ef44e33de3d6cc66f6bdbfece6a0988b70a6386235af66d161b03acd2`). A second encrypted off-host backup after ACK is `argus-prod-20260924-tymra-post-acceptance.sparseimage` (SHA-256 `dcff0607a1aa62f6e5d20d4b0e98a759116de56111e5092f338ffc6d4f56860a`). It was checksum-verified after a read-only remount; no separate post-ACK database restore was run. The passphrase remains in the existing `Argus production backup encryption` Keychain item for `argus.nz/argus-prod-20260924-tymra-client`.

Rollback materials are retained, but restoring the pre-acceptance Tymra database would discard accepted results. For a collection-runtime failure, first stop only Worker/API and restore the protected environment and prior image; preserve the accepted database and evidence volume. A database restore needs a separate assessment of the now-ACKed Argus Job and accepted Tymra business rows. Do not delete volumes, run a general seed, or re-capture Lincoln as a routine rollback.

## Deployment and bounded acceptance

- Before the release: only Web, PostgreSQL and Redis ran; the source was suspended; no Job was pending and no schedule was enabled. Argus had no active capture. The original Tymra database held 32 successful migrations and no `SourceMarketSignal`.
- Argus's evidence response now includes `Cache-Control: private, no-store, no-transform`. Through the actual `https://api.argus.nz` Cloudflare path, Node `fetch` returned the original HTML (1,384,924 bytes, SHA-256 `770e131392401c3d2fd76bbaae6a6143306e67792365170fdcdc2ad20057dd12`) and screenshot (1,015,016 bytes, SHA-256 `e7f5b5dfed198b0acd54e4f9876049a3a3463c228f82f7d44d935430b4f48b59`). These matched the Argus metadata and the response hash headers. No source-site visit was needed for this check.
- Exactly one new migration, `20260924021700_university_calendar_signal`, added the `UNIVERSITY_CALENDAR` enum. The production count is now 33 successful and zero failed migrations. No seed ran. The collection Compose render contained Web, PostgreSQL, Redis, migrate, Worker and API, with no Scheduler service. The disabled switches, hidden customer mode and Admin-only access were checked in the rendered Worker/API environment.
- Worker and API run the v3 image. Their Node clients reached Argus health/readiness (200/200); the scoped `tymra-prod` token received 404 for an absent Job and 403 for forbidden runtime access. Both could download the exact historic HTML bytes. The API's `/worker/health` returned 200 with database, Redis and Argus healthy, queue depth zero, failed Jobs zero and Scheduler disabled.
- `tymra_argus_evidence` mounted read/write at `/argus-evidence`. A probe file survived a forced Worker recreation in the same volume, then was removed. API remained healthy. No Scheduler container started.
- The existing, non-demo `christchurch_university_dates` source was resumed with exact database predicates and no health-check request to an external site. Its `lincolnAcceptanceOnly` guard remained true. The formal production preflight returned ready with no failures. Exactly the existing FAILED parent Job `cmuewbfvv0000pd4er93ffgny` was retried via `queue:history`; no second Argus Job was submitted.
- The sole Argus execution `job_767304e12cb4d49628e675a0749ff64a` used `lincoln-university-key-dates` for 2026, with 105 extracted dates and two evidence items. New Tymra CollectionRun `cmuezbjjr0001nm07wxjxyhqq` succeeded. The original failed run `cmuewbgjd0001s008v8q23qov` remains for audit. The production database has exactly two `UNIVERSITY_CALENDAR` `SourceMarketSignal` rows with distinct external IDs, both linked to the successful run. There is one ArgusExecution and no duplicate business record.
- Two distinct evidence files were copied into Tymra's persistent volume and re-hashed from disk. The old and new CollectionRuns each retain HTML and screenshot artifact references to those same files; four `tymra-evidence:` references therefore represent two files, not four business results. No `argus-evidence:` references remain for this parent Job.
- Tymra ACKed Argus at `2026-09-24T03:35:34.452Z`; Argus marked delivery `PURGED` at `03:35:34.477Z`. Authenticated GET of the result returned 410; GET of HTML and screenshot returned 404. The corresponding `/data/results/argus/<trace>` directory is absent. Argus Job audit status counts did not increase during the retry.

## Remaining boundaries

Scheduler and high-frequency Scheduler, new Check intake, internal on-demand, customer funnel, public registration, billing/Stripe, SMTP and membership launch flags remain off. `ops.tymra.nz` remains the Admin-only, noindex route; root and `www` were not opened by this release. The source has no enabled schedule and remains limited to the Lincoln acceptance job shape. RBNZ's previously reported production `PARSING_ERROR` was not retested or used as a gate. Other connectors, long-running scheduling, customer workflows and real OTA account/login state are not accepted by this run.
