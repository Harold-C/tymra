# Admin membership components

Operator-only customer, membership, risk and data-request actions live here. These components must
use the Admin session boundary and must never be imported into customer-facing routes.

