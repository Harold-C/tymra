# Member components

Authenticated customer navigation, account views and password sign-in/sign-up forms live here.
Anonymous marketing and Price Check funnel components remain in `components/public`; Admin-only
membership operations remain in `components/admin/membership`.

